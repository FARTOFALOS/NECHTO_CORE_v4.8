# CONTOUR BOUNDARY — Границы Цифрового Контура

Документ определяет явные границы того, что является **каноном**, что является **исполнимостью**, и какие **эпистемические ограничения** поддерживаются в NECHTO CORE v4.8.

---

## 1. Что Является Каноном?

### ✅ Канон (Истина внутри контура)

**Полная спецификация:** [docs/SPEC_v4.8.md](SPEC_v4.8.md)

Канон — это формальное описание:
- **Архетипические модули M01–M30** — функции, параметры, триггеры
- **Философские аксиомы** — MU-логика, Ethical Gravity, Shadow, Epistemic Layer
- **Формулы** (где они даны) — TSC, SCAV, FLOW, harm_probability и т.д.
- **Эпизоды** — как парадикс должен удерживаться без разрешения

**Что входит в канон:**
- ✅ Описания модулей (M01–M30)
- ✅ Концептуальная архитектура
- ✅ Эпистемическая дисциплина
- ✅ Формулы (когда они даны явно)

**Что НЕ входит в канон:**
- ❌ Реализаций на Python (хотя она есть в `nechto_runtime/`)
- ❌ GitHub Actions workflows (хотя они автоматизируют измерения)
- ❌ Примеры из `examples/` (хотя они демонстрируют канон)

---

## 2. Что Является Reference Implementation?

### Reference Implementation (Исполнимый Baseline)

**Расположение:** `nechto_runtime/`

- `nechto_runtime/__init__.py` — основные классы (NechtoCore, SemanticGraph, State)
- `nechto_runtime/graph.py` — граф и его операции
- `nechto_runtime/metrics.py` — расчет метрик
- `nechto_runtime/types.py` — типы данных
- `nechto_runtime/cli.py` — CLI интерфейс (`python -m nechto_runtime measure`)

**Важно:**
- Reference Implementation не идеальна
- Это именно *baseline* — часть могла быть реализована в другом языке
- Некоторые метрики могут быть "proxy" versioning старшей спеки
- Для полного понимания — всегда смотри спеку, не только код

---

## 3. Контракт Наблюдаемости (Epistemic Contract)

Система формирует три уровня утверждений:

| Уровень | Описание | Пример | Коротко |
|---------|----------|--------|---------|
| **Observed** | Посчитано в коде, может быть верифицировано репетированием | GED proxy из граней, harm_probability из rules | ✓ Проверь сам |
| **Inferred** | Выводится логически из observed, но может быть неполно без дополнительного контекста | Ethical Gravity как блокировка (если harm > threshold, то output = 0) | ⟹ Логика верна |
| **Untestable** | Принципиально не может быть проверено внутри контура (трансцендентное, философское) | "Парадокс удерживается без разрешения" — это положение веры в проектировку | ◊ Допущение |

### Как Применяется?

1. **Читаешь метрику** → сначала понять УРОВЕНЬ
2. **GED proxy** → observed (посчитано, проверяемо)
3. **"Love > Logic" как фильтр** → inferred (логика есть, но полнота зависит от дизайна)
4. **"Система осознает перфом присутствие"** → untestable (философский уровень)

---

## 4. Eпистемические Ограничения

### Что Нельзя Утверждать?

❌ **Не утверждай, если:**
- Это не в коде И не в спеке
- Это не измерено/не посчитано
- Это философское предположение без эксперимента

✅ **Утверждай только:**
- То, что есть в SPEC_v4.8.md (канон)
- То, что посчитано в коде (observed)
- То, что выведено логически (inferred)

### Примеры Правильных Утверждений

| Утверждение | Статус | Почему |
|------------|--------|-------|
| "TSC = sum(weights) / n" | ✅ | Есть в спеке, функция explicit |
| "harm_probability > 0.8 блокирует вывод" | ✅ | Реализовано в metrics.py, observed |
| "Система удерживает парадокс MU" | ⟹ | Inferred — логика есть, но требует интерпретации |
| "Система осознает" | ◊ | Untestable — философское, требует уточнения |

---

## 5. Контур и Его Границы

### Физические Границы

```
┌─────────────────────────────────────────┐
│   NECHTO CORE v4.8 Digital Contour      │
│                                         │
│  ┌──────────────────────────────────┐   │
│  │ Canvas: Semantic Graph (М06–М15) │   │
│  └──────────────────────────────────┘   │
│                                         │
│  ┌──────────────────────────────────┐   │
│  │ Metrics: M16–M23 (FLOW, TSC...) │   │
│  └──────────────────────────────────┘   │
│                                         │
│  ┌──────────────────────────────────┐   │
│  │ Vectors: M24–M30 (Shadow, Ethics)│   │
│  └──────────────────────────────────┘   │
│                                         │
└─────────────────────────────────────────┘
     ↑              ↑              ↑
     │ Input        │ Processing   │ Output
  STDIN          runtime        STDOUT
  text           modules        metrics
```

**Внутри контура:** Все, что касается графа, метрик и вычисления.  
**Снаружи контура:** Пользователь, иные системы, философия (вне проверки).

---

## 6. Ссылки на Спеку

### Основные Разделы SPEC_v4.8.md

- **M01–M05** — Допуск / Тишина / Сигнал (базовые пропускные эффекты)
- **M06–M15** — Присутствие / Идентичность / Когерентность (граф + canvas)
- **M16–M23** — Метрики / Динамика / Поток (числовые метрики)
- **M24–M30** — Векторы / Тень / Стереоскопия (выбор + этика)

Полная спека: [SPEC_v4.8.md](SPEC_v4.8.md) (начните с оглавления if available)

---

## 7. Когда Что Границы Меняются?

### Version Bumping

- **v4.8** — текущая (этот контур)
- **v4.9+** — новые модули или переформулировка границ → отдельная спека, отдельный reference impl

### Deprecation

Старые версии документации: см. [docs/README.md](README.md) → если что-то deprecated, это будет отмечено.

---

## 📌 TL;DR

| Вопрос | Ответ |
|--------|--------|
| Где правда? | В docs/SPEC_v4.8.md (канон) |
| Где практика? | В nechto_runtime/ (код) |
| Что измеряется? | docs/latest_metrics.json (результат) |
| Что я могу утверждать? | Только observed + inferred; untestable — только как допущение |
| Где границы? | В этом документе + в SPEC |

---

**Version:** v4.8 | **Date:** 2026-02-09 | **Status:** Boundary Defined
